#include <bits/stdc++.h>
using namespace std;

#ifndef DEBUG
#define deb(n)
#define debar(ar, size)
#else
#define deb(n) cout << #n << " = " << (n) << '\n'
#define debar(ar, size) cout << #ar << " = "; for (ll i = 0; i < size; i++) cout << ar[i] << ' '; cout << '\n'
#endif

#define UwU ios_base::sync_with_stdio(0),cin.tie(0)
#define nl '\n'
#define skipline() cin.ignore(numeric_limits<streamsize>::max(), nl)
#define yes "YES"
#define no "NO"
typedef unsigned long long ull;
typedef long long ll;
#define FOR(i, init, n) for (ll i = (init), __ = (n); i < __; i++)
#define REP(n) for (ll _ = (n); _--;)

void solve()
{
}

int main()
{
	UwU;
	int ts = 1;
	// cin >> ts;
	for (; ts--; ) {
		solve();
	}
}
