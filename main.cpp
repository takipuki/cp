#include <bits/stdc++.h>
using namespace std;

#ifdef DEBUG
#include "debug.cpp"
#else
#define deb(...)
#endif

#define UwU ios_base::sync_with_stdio(0),cin.tie(0)
#define nl '\n'
#define skipline() cin.ignore(numeric_limits<streamsize>::max(), nl)
#define yes "yes"
#define no "no"
using ull = unsigned long long;
using ll = long long;
using ii = pair<int, int>;
using vi = vector<int>;
using vvi = vector<vector<int>>;
using vl = vector<ll>;
using vvl = vector<vector<ll>>;
#define FOR(i, init, n) for (int i = (init), __ = (n); i < __; i++)
#define ROF(i, init, n) for (int i = (init), ___ = (n); i >= ___; i--)
#define REP(n) for (ll _ = (n); _--;)

void solve()
{
}

int main()
{
	#ifndef DEBUG
	UwU;
	#endif
	int ts = 1;
	cin >> ts;
	for (; ts--; ) {
	// for (cin >> n; n; cin >> n) {
	// for (int t = 1; t <= ts; t++) {
		// cout << "Case " << t << ": ";
		solve();
	}
}
